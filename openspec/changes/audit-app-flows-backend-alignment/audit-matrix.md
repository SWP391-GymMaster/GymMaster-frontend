# Audit Matrix - Verified P0/P1 Pass

> Status: Updated after repo inventory and sanitized live backend probes. Backend base is reachable, but public Swagger/OpenAPI discovery returned `404` at common paths. Đây chưa phải full live RBAC/mutation audit vì backend demo accounts/tokens không có sẵn trong repo và mock credentials không authenticate được trên backend thật.

## Priority Rubric

| Priority | Meaning | Review rule |
|---|---|---|
| P0 | Blocker/security/demo-core integration risk | Review first, fix or verify before demo |
| P1 | High risk for MVP module or backend contract | Review after P0, usually needs scoped audit/fix |
| P2 | Medium consistency/secondary/test-confidence risk | Schedule after core flow is stable |
| P3 | Low housekeeping or planning hygiene | Defer unless quick and safe |

## Evaluation Table

| Priority | Module / Flow | Evidence | Risk / Concern | Suspected Owner | Verification | Recommendation |
|---|---|---|---|---|---|---|
| P0 | Google OAuth login | Live probe: `POST /api/v1/auth/google` with dummy token returned `500`, `success=false`, `GOOGLE_NOT_CONFIGURED`; `src/features/auth/components/GoogleLoginButton.tsx`; `src/features/auth/api/auth.api.ts` | FE reaches backend and backend reports missing Google ClientId. Login with Google will fail outside mock mode. | Config / BE | Configure backend Google ClientId/allowed origins; retest with real Google ID token. | Treat as backend/config blocker, not FE setup, unless backend expects a different payload than `{ idToken }`. |
| P0 | API contract source coverage | `contract-inventory.md`; `live-backend-probe.md`; public Swagger/OpenAPI discovery returned `404`; most endpoints are inferred from `src/features/**/api` and `src/mocks/handlers/*.ts` | FE and MSW can pass while live backend paths/payloads/status codes differ. Live probes found many registered protected routes via empty 401, but still cannot confirm DTOs/business rules without tokens/docs. | Both / Docs | Provide Swagger/OpenAPI, Postman/Bruno collection, backend route list, or enable docs endpoint; compare method/path/request/response/error per MVP flow. | Make backend contract inventory the first dependency before broad fixes; current base alone is enough for safe route existence probes, not full contract extraction. |
| P0 | RBAC and resource ownership | `src/features/auth/components/PermissionGuard.tsx`; `src/mocks/utils/api-response.ts`; docs require PT assigned-only and Member self-only; live mock credentials returned `INVALID_CREDENTIALS` | UI guard and MSW checks exist, but live server-side ownership could not be verified without valid role accounts/tokens. FE alone cannot guarantee security. | BE / Both | Provide valid Admin/Staff/PT/Member demo accounts or short-lived test tokens; live-test forbidden access. | Keep as P0 blocked until server-side RBAC is verified. |
| P0 | Protected error envelope mismatch | Live probes for protected endpoints returned `401` with empty body; FE mitigation added in `handle-empty-protected-api-errors` to normalize empty `401/403` in `src/lib/api/http-client.ts` | Frontend no longer has to surface empty protected failures as `INVALID_RESPONSE`, but backend still loses envelope metadata such as `requestId`. | BE / FE | Validate the FE fallback with tests, then decide whether backend should also return JSON envelopes for all `401/403` responses. | Keep backend envelope consistency as a contract recommendation; FE fallback reduces demo risk for protected flows. |
| P1 | Central API client/session refresh | `src/lib/api/http-client.ts`; feature APIs manually pass `Authorization`; refresh logic in `src/features/auth/session/auth-session.ts`; design doc says client should handle token attach/refresh/error normalize | No global token attach/401 refresh in API client; inconsistent handling can cause random 401/session expiry behavior across feature queries. | FE | Requires valid expired token/refresh token to live-test; currently blocked by lack of live seed credentials. | Audit whether to centralize auth headers + 401 handling or keep explicit per feature with documented convention. |
| P1 | Staff sell/renew/payment | `src/features/staff-front-desk/api/staff-front-desk.api.ts`; `src/mocks/handlers/billing.handlers.ts`; `docs/init/06_FEATURE_SPECS.md` | Business rules need PendingPayment -> Active, duplicate payment 409, renew end date nối tiếp, AuditLog. FE endpoint assumptions may not match backend. | Both | Compare `/memberships/sell`, `/:id/payment`, `/:id/renew` with backend; run sell -> payment -> check-in flow against live API. | Audit this as first core ops flow after auth/RBAC. |
| P1 | Check-in | `src/features/staff-front-desk/api/staff-front-desk.api.ts`; `src/mocks/handlers/checkin.handlers.ts`; `docs/init/03_SRS_USE_CASES.md`; missing `/member/check-in` page | Staff check-in exists; member check-in route is in source-of-truth but page not present. Backend must reject expired/locked/pending-payment member. | Both | Verify staff check-in live responses for active, expired, pending payment, locked, not found; confirm member self check-in scope. | Separate Staff terminal P1 from Member self check-in route gap P1/P2 depending MVP decision. |
| P1 | Route coverage vs source-of-truth | `contract-inventory.md`; `docs/design/08_ROUTE_MAP_NAVIGATION.md`; `AGENTS.md`; `find src/app` output | Covered: PT nested `workout/notes/progress` now exists. Still missing/staged: `/admin/audit-logs/[id]`, `/pt/members/[id]/nutrition`, `/member/check-in`, `/member/nutrition`, `/member/nutrition/food-search`, `/member/profile`. | FE / Product | Confirm which missing routes are intentionally staged, redirected, or required before demo. | Convert owner decisions into route-specific follow-up specs. |
| P1 | PT assignment | `src/features/pt-assignment/api/pt-assignment.api.ts`; `src/mocks/handlers/training.handlers.ts`; `docs/init/03_SRS_USE_CASES.md` | Duplicate active assignment must return 422 `ALREADY_ASSIGNED`; candidate endpoints and payload shape need backend confirmation. | Both | Live-test candidate member/trainer filters and duplicate assignment error. | Audit assignment before PT training because training depends on assigned members. |
| P1 | PT training and member-owned training views | `src/features/pt-training/api/pt-training.api.ts`; `src/mocks/handlers/training.handlers.ts` | Paths include PT member-specific and member self endpoints (`/members/me/...`). Backend may expose different REST shape; RBAC is critical. | Both | Verify create/list workout plan and trainer note as PT; verify member can only read self plan/note. | Audit after assignment contract is confirmed. |
| P1 | Dashboard and audit log | `src/features/admin-dashboard/api/admin-dashboard.api.ts`; `src/mocks/handlers/dashboard-audit.handlers.ts`; docs require real records and audit logs for mutating actions | Dashboard must reflect real payments/memberships/check-ins, not static mock; AuditLog must be written by backend after key mutations. | BE / Both | Run sell/payment/check-in/assign actions live, then fetch dashboard/audit logs and compare deltas. | Treat as demo confidence gate once core mutations work. |
| P1 | MSW backend-contract test baseline | Initial targeted Vitest exposed 4 failing contract tests from `GM-101` id drift; fixed by normalizing mock member profile id and member session test setup to `101`; targeted reruns pass | Contract tests for sell/payment, duplicate assignment, profile-360, progress, and nearby member/nutrition/staff flows are green again. | FE Test | Keep these targeted tests as a regression gate for future backend-contract work. | Resolved in this pass; no follow-up unless broader suite finds additional drift. |
| P2 | Nutrition online/barcode proxy | `docs/backend/online-food-search-integration.md`; `src/features/member-nutrition/api/member-nutrition.api.ts`; `src/features/member-nutrition/api/nutrition-api.ts`; live v1 and docs-style online/barcode paths returned `404` | Some code calls backend proxy with raw relative `fetch('/api/v1/...')`, then falls back to direct Open Food Facts/USDA. Neither `/api/v1/food-items/...` nor `/api/food-items/...` online/barcode routes are exposed at the configured base. | FE / Both | Confirm whether backend has implemented online/barcode proxy, canonical path, and response shape; test with real token after route exists. | Decide whether external fallback is dev-only and route all runtime calls through `apiRequest` + backend proxy. |
| P2 | Member 360 endpoint canonicalization | `src/features/member-360/api/member-360.api.ts`; `src/mocks/handlers/member-360.handlers.ts` | FE uses `/members/:id/profile-360`; mock also supports `/members/:id/360`. Two names can hide backend mismatch. | Both | Ask backend for canonical endpoint and remove/mark alias in mock after confirmation. | Canonicalize contract before live integration tests. |
| P2 | Progress field naming and ownership | `src/features/member-progress-tracking/api/member-progress.api.ts`; `docs/init/15_DATABASE_SCHEMA.md` | Code types may use FE-friendly fields while DB/docs use names like log date/body metrics. Backend DTO mapping must be explicit; PT/member ownership must be verified. | Both | Compare DTO fields against backend response and test PT assigned-only/member self-only progress create/list. | Add DTO contract notes or adapter tests if backend shape differs. |
| P2 | Notifications | `src/features/notifications/api/notifications.api.ts`; `src/mocks/handlers/notifications.handlers.ts`; product marks notification as secondary | Notification UI/API exists, but backend contract is likely not core MVP. Querying by role can become insecure if backend trusts client role param. | FE / BE | Confirm whether backend supports notifications for MVP and whether role param is ignored/validated server-side. | Keep secondary unless it affects shared notification drawer in demo. |
| P2 | Runtime design consistency | `src/features/auth/components/GoogleLoginButton.tsx`; `package.json`; `AGENTS.md` Lucide-only rule | `@tabler/icons-react` is used for Google icon while repo rule says Lucide only. This is low integration risk but design-rule drift. | FE | Decide whether brand icon exception is allowed. | Fix later with an approved design consistency task if desired. |
| P2 | Vietnamese/runtime copy drift | Broad codebase; AGENTS runtime copy rule | Some legacy English technical labels/messages may remain. Mixed language can reduce demo polish. | FE | Run copy scan after core contract audit. | Schedule a focused copy task, not mixed into backend-alignment fixes. |
| P3 | OpenSpec artifact tracking | `.gitignore`; `git status --ignored=matching openspec/...` shows ignored change dir | `openspec/` artifacts may be invisible in git/PR review unless explicitly tracked or unignored. | Docs / Workflow | Confirm owner preference for local-only vs repo-tracked OpenSpec. | Decide before relying on OpenSpec artifacts for team review. |
| P3 | `.env.example` deleted in worktree | `git status --short` shows `D .env.example` before this audit | Environment onboarding can be unclear, but this deletion appears pre-existing and unrelated. | Workflow | Ask owner whether deletion is intentional; do not restore silently. | Handle separately if env docs are needed for Google/API setup. |

## Suggested Review Order

1. P0 backend inputs: enable/provide Swagger/OpenAPI or route list, valid role demo accounts/tokens, Google backend config.
2. P0 protected error envelope follow-up: FE empty-401/403 handling is being mitigated; backend JSON envelopes for protected errors are still recommended.
3. P0 server-side RBAC live verification with real role accounts/tokens.
4. P1 Staff core ops: sell package, record payment, renew, check-in.
5. P1 Admin/PT chain: assign PT, PT member access, workout plan, trainer notes.
6. P1 Dashboard/audit log delta after real mutations.
7. P1/P2 route coverage decisions, especially member check-in/profile/nutrition pages.
8. P2 nutrition online/barcode backend proxy cleanup.
